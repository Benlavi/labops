services:
  labops-server:
    image: ghcr.io/benlavi/labops:${IMAGE_TAG:-latest}
    container_name: labops-server
    ports:
      - "8000:8000"
    environment:
      - LABOPS_DB_PATH=/data/labops.db
    volumes:
      - labops-data:/data

volumes:
  labops-data: